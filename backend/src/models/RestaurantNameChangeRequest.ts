import mongoose, { Document, Schema } from 'mongoose';
export interface IRestaurantNameChangeRequest extends Document { restaurant:mongoose.Types.ObjectId;restaurantId:string;oldName:string;requestedName:string;approvedName?:string;requestedBy:mongoose.Types.ObjectId;approvedBy?:mongoose.Types.ObjectId;reason:string;status:'pending'|'approved'|'rejected';requestedAt:Date;approvalDate?:Date }
const schema=new Schema<IRestaurantNameChangeRequest>({restaurant:{type:Schema.Types.ObjectId,ref:'Restaurant',required:true,index:true},restaurantId:{type:String,required:true,immutable:true},oldName:{type:String,required:true,immutable:true},requestedName:{type:String,required:true,trim:true,maxlength:100,immutable:true},approvedName:{type:String},requestedBy:{type:Schema.Types.ObjectId,ref:'User',required:true,immutable:true},approvedBy:{type:Schema.Types.ObjectId,ref:'User'},reason:{type:String,required:true,trim:true,maxlength:500,immutable:true},status:{type:String,enum:['pending','approved','rejected'],default:'pending',index:true},requestedAt:{type:Date,default:Date.now,immutable:true},approvalDate:Date},{timestamps:true});
schema.index({restaurant:1,status:1},{unique:true,partialFilterExpression:{status:'pending'}});
export const RestaurantNameChangeRequest=mongoose.model<IRestaurantNameChangeRequest>('RestaurantNameChangeRequest',schema);
